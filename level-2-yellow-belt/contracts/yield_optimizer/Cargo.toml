[package]
name = "yield_optimizer"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "20"

[dev_dependencies]
soroban-sdk = { version = "20", features = ["testutils"] }
